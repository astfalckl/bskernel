% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bspline.R
\name{evaluate_bspline_basis}
\alias{evaluate_bspline_basis}
\title{Evaluate a Normalised B-spline Basis Function}
\usage{
evaluate_bspline_basis(omega, knots, i, k)
}
\arguments{
\item{omega}{A numeric vector of input locations at which to evaluate the basis function.}

\item{knots}{A numeric vector of knots.}

\item{i}{An integer index indicating the position in the knot sequence for which to compute the coefficients.}

\item{k}{The degree of the B-spline basis function (must be 0, 1, or 2).}
}
\value{
A numeric vector of the same length as \code{omega}, giving the evaluated basis function values.
}
\description{
Evaluates the B-spline basis function of degree \code{k}, indexed by \code{i},
at a vector of input locations \code{omega}, using its truncated power expansion.
The result is scaled so that the basis integrates to 1 over its support.
}
\examples{
knots <- c(0, 1, 2, 3, 4, 5)
omega <- seq(0, 5, length.out = 100)
y <- evaluate_bspline_basis(omega, knots, i = 1, k = 1)
plot(omega, y, type = "l")

}
